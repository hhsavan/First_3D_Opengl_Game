#ifndef HELPERS_HPP
#define HELPERS_HPP
#include <iostream>

#include <glad/glad.h>
#include <GLFW/glfw3.h>

void updateTriangle(float* verticies,const int count);
// void key_callback(GLFWwindow* window, int key, int scancode, int action, int mods);

// float* deger;
#endif